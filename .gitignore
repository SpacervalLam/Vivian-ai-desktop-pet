# ===== Rust / Tauri =====
src-tauri/target/
target_check/
src-tauri/gen/schemas/
src-tauri/cargo_check*.txt

# ===== Node.js / Frontend =====
node_modules/
dist/
dist-ssr/
*.local

# ===== Vite =====
*.tsbuildinfo

# ===== IDE / Editor =====
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ===== Tauri WebView 缓存 =====
.webview-data/

# ===== 环境变量与密钥 =====
.env
.env.*
!.env.example

# ===== 日志与运行时数据 =====
*.log
logs/
src-tauri/logs/

# ===== 构建产物 =====
*.exe
*.dll
*.lib
*.pdb
*.tmp

# ===== 用户运行时数据（不应入库） =====
vivian_data/
# 工作侧智能体在「选定工作区」下生成的跨会话项目记忆（<工作区>/.vivian/memory.md）
# 每个工作区各一份、由 LLM 每轮重写，属本地运行时状态，不入库
.vivian/

# ===== 构建期脚本（本地工具，不入库） =====
# 注意：encrypt-assets / doctor / check-* 等构建与哨兵脚本都在这里，
# ignore 后新 clone 无法直接构建，需自行补齐 scripts/
scripts/

# ===== 加密资源与密钥（构建产物，不入库） =====
src-tauri/assets_enc/
src-tauri/asset_key.bin
src-tauri/vivian.bundle.enc
src-tauri/vivian.bundle.index.json

# ===== AI 会话产物=====
.workbuddy/
.workbuddy-ai/
MEMORY_OPTIMIZATION.md
.texture-backup-4096/
.glb-backup-4096/
.config-backup-*/

# ===== 本地临时区（脚本中间产物 / 归档 / 缓存，不入库） =====
tmp/
.npm-cache/
.npm-cache-tmp/

# ===== Blender 美术工程（开发链路，不入库 / 不进包） =====
# 应用运行只需要 public/room/models/vivian-furniture.glb（进 dist → 进安装包）。
# /blender/ 下的 .blend 源工程、中间 glb、导出贴图、验证截图全部属于开发过程文件，
# 构建链为：export-base.mjs → build_blender.py → export_blender.py → verify-room.mjs
#
# 代价须知：忽略后 vivian-furniture.blend 将不再有任何版本管理，仅存本机一份。
# 若要保留该源工程入库，把 /blender/ 那行注释掉，并保留下面一行反选：
#   !/blender/room/vivian-furniture.blend
/blender/
*.blend1
*.blend2

# ===== 开发调试残留（仅本地，不入库） =====
# src-tauri 根目录的 txt 都是 cargo check / cargo test 的输出重定向，源码不放这里
src-tauri/*.txt
# cargo test 在工作目录生成的向量库与状态快照
# （见 src/memory/strategy.rs 与 src/psychology/social_state.rs 的测试模块）
src-tauri/test_vectors.json
src-tauri/test_social.json
# 无任何引用的散落测试文件、锁文件副本
src-tauri/test_sqlite.rs
src-tauri/Cargo.lock.tmp
