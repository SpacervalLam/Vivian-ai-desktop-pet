# 环境变量示例
# 复制此文件为 .env 并按需填写
#
# 注意：Vivian 的 LLM API Key / Endpoint / Model 均通过配置文件管理
#   （%APPDATA%\Vivian\config.yaml 的 routing_matrix 与 providers 字段）
#   不通过环境变量读取。本文件仅列出代码实际会读取的环境变量。

# 代理设置（可选，仅在 config.yaml 的 network.proxy_mode = "system" 时生效）
# 代码读取优先级：HTTPS_PROXY > https_proxy > HTTP_PROXY > http_proxy
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890
