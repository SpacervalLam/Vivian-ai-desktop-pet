# 陪伴记忆 V2

## 唯一写入来源

| 层 | 生产者 | 内容 | 保存与读取 |
| --- | --- | --- | --- |
| 原始对话 | 用户/角色消息 | 原话和角色归属 | DialogueManager 历史与 ShortTerm 缓冲；近轮直接入上下文，后台按主题摘要 |
| 共同经历 | Stage 1 | 0–3 条主题摘要，包含明确说过的事和可延续背景 | SessionSummary；按时间、主题检索；空内容消费原话，不制造事件 |
| 长期事实 | AutoExtractor | 身份、偏好与边界、项目、明确约定、重要关系事件 | LongTerm；每项必须有对话原文引文，支持 ADD/UPDATE/DELETE、去重和未完成约定 |
| 实时状态 | PsychologyManager / Mind | 关系数值、情绪、当前想法、搭话候选 | 当前想法与搭话候选仅在运行时；关系数值单独持久化，不复制成叙事事实 |

会话摘要不再重新提取画像、长期事实或洞察。主回复和反思不再各自生成事件及长期事实。用户事实卡保留手动编辑，不再从相同对话另行抽取。AutoExtractor 的一次调用同时判断类别和证据；写入时不再逐条调用增强模型。当前想法最多保留最近六条、十分钟，用于内心独白的短期上下文。

## 召回与回复

近轮原话优先。普通陪伴回复最多注入五条来源明确且去重的记忆；主动搭话最多三条。长期事实以用户最新的明确更正为准。摘要只接续脉络，不当成用户身份或关系升级的独立证据。回复自然使用相关记忆，不为展示记忆而强行提及。

## 主动对话

每次生成 current_thought 时，同一次 LLM 输出社交意愿、具体话题和逐字依据。没有可核验依据时不产生候选。候选有效期两分钟；用户在场、未聊天、空闲 90–240 秒、时机评分合格时提高 Spontaneous 的触发概率。仍受全局冷却、退避和安静模式限制。发言后消费话题，避免重复。普通问候不因抽象 social_urge 提前触发，过期信号不再阻止问候。

## 存储边界

新陪伴数据保存在 `characters/<id>/companion-v2/` 与全局 `companion-v2/`。角色设定、日记、配置仍在原目录。旧版记忆文件不会被 V2 读取。旧文件的物理删除在本次任务中被本机自动审批拦截，需在允许删除的环境里单独处理。

建议观察重复事实数、每轮记忆提示词 token、主动消息被忽略率、事实更正后的召回，以及当前想法候选的实际发送率。真实模型输出质量仍需上线后的对话验证。
